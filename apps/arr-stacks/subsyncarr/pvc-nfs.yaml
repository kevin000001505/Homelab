apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-nfs
  namespace: arr-stacks
spec:
  storageClassName: ""
  volumeName: media-nfs
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 10Ti
