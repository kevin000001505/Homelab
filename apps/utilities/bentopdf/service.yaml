apiVersion: v1
kind: Service
metadata:
  name: bentopdf
  namespace: utilities
  labels:
    app: bentopdf
    tier: utilities
spec:
  type: ClusterIP
  selector:
    app: bentopdf
    tier: utilities
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
