apiVersion: v1
kind: PersistentVolume
metadata:
  name: dawarich-db-data-pv
spec:
  volumeMode: Filesystem
  hostPath:
    path: /appdata/dawarich/db-data
    type: DirectoryOrCreate
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
