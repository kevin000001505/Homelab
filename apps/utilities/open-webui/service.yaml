apiVersion: v1
kind: Service
metadata:
  name: open-webui
  namespace: utilities
  labels:
    app: open-webui
    tier: utilities
spec:
  type: ClusterIP
  selector:
    app: open-webui
    tier: utilities
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
